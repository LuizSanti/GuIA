// config.js — NÃO armazena mais a chave aqui.
// A chave fica no servidor, no arquivo .env (nunca commitado).
// Este arquivo pode ser commitado com segurança.
const CONFIG = {
    USE_BACKEND_PROXY: true   // o frontend vai chamar /api/groq em vez da Groq diretamente
};